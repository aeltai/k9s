# rk9s hotkeys - custom shortcuts (plugins add these automatically)
# Press ? to see RESOURCE section for view-specific plugin shortcuts
#
# rk9s plugin shortcuts (when viewing matching resource):
# NODES:    Shift-C RKE2/K3s config | Shift-D services | Shift-P crictl | Shift-E etcdctl
# RANCHER:  Shift-O UI | Shift-F Fleet UI | Shift-K sync kubeconfig | Shift-J projects | Shift-U clusters
# FLEET:    Shift-G GitRepo status | Shift-R reconcile | Shift-T bundle target
# LONGHORN: Shift-L Longhorn UI | Shift-V volume YAML | Shift-T trim
# HARVESTER: Shift-H UI
# VIRTCTL:   Shift-S console | Shift-V VNC | Shift-W start | Shift-X stop | Shift-Z restart
#            Shift-P pause | Shift-Q unpause | Shift-Y SSH | Shift-I guest info | m migrate
# KUBEWARDEN: Shift-K policies | Shift-I inspect | Shift-S scaffold
# INGRESS:   Shift-I Traefik/NGINX describe
#
hotKeys:
  # Examples...
  # shift-0:
  #   shortCut: Shift-0
  #   description: View Workloads
  #   command: wk k8s-app=cilium